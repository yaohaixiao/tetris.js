// 清理层数的得分
const CLEAR_LINE_SCORES = [0, 100, 300, 500, 800, 1200];
// 游戏字体
const FONT_FAMILY = `"Press Start 2P", monospace, sans-serif`;

const GAME = {
  CLEAR_LINE_SCORES,
  FONT_FAMILY,
};

export default GAME;
