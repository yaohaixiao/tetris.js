// 清理层数的得分
const CLEAR_SCORES = [0, 100, 300, 500, 800];
// 游戏字体
const FONT_FAMILY = `"Press Start 2P", monospace, sans-serif`;
// 最大级别
const MAX_LEVEL = 99;

const GAME = {
  CLEAR_SCORES,
  MAX_LEVEL,
  FONT_FAMILY,
};

export default GAME;
