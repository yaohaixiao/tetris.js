import startHiddenMode from '../core/start-hidden-mode.js';

// 按住 P 键开始计时（触发隐藏模式）
const executeStartHiddenModeCommand = () => {
  startHiddenMode();
};

export default executeStartHiddenModeCommand;
